[package]
name = "raster-editor"
version = "0.0.0"
description = "The Raster editor: the shell that holds every dedicated editor."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
raster-core = { path = "../raster-core" }
raster-math = { path = "../raster-math" }
raster-input = { path = "../raster-input" }
raster-render = { path = "../raster-render" }
raster-ui = { path = "../raster-ui" }
toml = "0.9"

[dev-dependencies]
raster-core = { path = "../raster-core" }
