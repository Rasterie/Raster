[package]
name = "raster-ui"
version = "0.0.0"
description = "The UI toolkit for the Raster engine: text, layout, widgets."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
raster-core = { path = "../raster-core" }
raster-math = { path = "../raster-math" }
raster-input = { path = "../raster-input" }
raster-render = { path = "../raster-render" }
